package postedLinkedInQuestions;

/**
 * 给定一个undirected-graph和一个s节点和一个d节点，判断s和d的距离是否<=3。距离定义
 * 为s和d之间最短路径上link的数目。如果d是s的邻居，则距离为1。 注意，这个undirected-graph
 * 使用adjacent-array来表示一个节点的所有neighbors，并且每个节点最多有n个neighbors。每个
 * 节点都有一个Idx，并且每个节点的adjacent-array都是sorted。例如1有邻居2和3，那么1的
 * adjacent array是[2,3] (sorted) 直接的BSF解法时间复杂度是O(n^3)。 要求设计Solution
 * 是时间 O(n^2)。
 */

public class PathInUndirectedGraph {

//    public static boolean findPath(Graph g, Node a, Node b){
//
//    }
}
